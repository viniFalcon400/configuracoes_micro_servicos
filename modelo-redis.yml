server:
  port: 8082

spring:
    cache:
        type: redis
    redis:
        host: localhost
        port: 6379
    datasource:
        url=jdbc: h2:mem:db
    jpa:
        show-sql: true